package com.sln.dao.shop.write.operate;

import com.sln.entity.operate.OperationManager;
import org.springframework.stereotype.Repository;

@Repository
public interface OperationManagerWriteDao {
    Integer insert(OperationManager manager);
    Integer update(OperationManager manager);
    Integer updateStatus(OperationManager manager);

}
